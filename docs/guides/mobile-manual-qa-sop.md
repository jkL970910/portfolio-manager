# 手机端手动 QA SOP

最后更新：2026-04-28

## 测试入口

- 远程预览 URL：`https://peers-declaration-these-hello.trycloudflare.com`
- 目标设备：Android 手机 Chrome 优先；iOS Safari/Chrome 作为兼容性补测。
- 测试范围：Flutter Web 手机端当前已实现功能。
- 非测试范围：旧 Next.js Web 页面完整回归、CSV 导入、生产部署稳定性。

## 覆盖率口径

当前 Flutter mobile 已实现的主要功能域按 14 个计算：

1. 登录与会话恢复
2. 底部导航与基础移动端布局
3. 总览页
4. 组合页与分布卡片
5. 账户详情与账户编辑/删除入口
6. 持仓详情与持仓编辑/删除入口
7. 标的详情与标的身份隔离
8. Health Score 与资产类别下钻
9. 独立标的发现/搜索与观察列表操作
10. 推荐页、推荐生成、观察列表
11. 手动导入账户
12. 手动导入持仓与标的搜索/验证
13. 设置页、显示币种、行情刷新
14. 投资偏好、引导式偏好、推荐约束、AI 快扫

本 SOP 覆盖 13 个功能域，目标实际覆盖率约 `13 / 14 = 93%`。未完全覆盖的是真实跨设备长期会话和生产级 tunnel 稳定性，因为它们依赖环境而不是当前功能代码。

## 测试前提

- 远程开发栈已启动，Flutter Web、Next API、Cloudflare Tunnel 均可访问。
- 使用项目已有测试账号登录，不要在文档或截图中记录密码。
- 手机浏览器先关闭旧页面，再重新打开最新 URL。
- 如果页面异常，先刷新；仍异常再清理站点缓存后重试。
- 所有失败项记录：设备型号、浏览器、测试账号、步骤编号、实际结果、截图。

## 结果标记

- `Pass`：结果符合预期。
- `Fail`：结果不符合预期，需要修复。
- `Blocked`：环境、账号、网络或数据缺失导致无法判断。
- `N/A`：当前数据集不包含该场景，需说明原因。

## P0 核心验收用例

| ID | 功能域 | 场景 | 步骤 | 预期结果 |
| --- | --- | --- | --- | --- |
| MQA-001 | 启动 | 打开手机 URL | 手机浏览器打开测试入口 URL。 | Flutter 页面正常加载；没有 `failed to load`、530、白屏或无限 loading。 |
| MQA-002 | 登录 | 正常登录 | 输入测试账号邮箱和密码，点击登录。 | 进入主界面；底部出现 `总览 / 组合 / 推荐 / 导入 / 设置`。 |
| MQA-003 | 登录 | 错误密码 | 使用错误密码登录。 | 显示中文错误；页面不崩溃；可重新输入。 |
| MQA-004 | 会话 | 刷新恢复 | 登录后刷新浏览器页面。 | 自动恢复会话或 refresh token 后回到主界面。 |
| MQA-005 | 导航 | 底部导航切换 | 依次点击 `总览`、`组合`、`推荐`、`导入`、`设置`。 | 每个 tab 都能打开；没有横向溢出；底部导航不遮挡关键内容。 |
| MQA-006 | 总览 | 总览页数据 | 打开 `总览`。 | 显示 Loo国总览、核心指标、重点账户、头部持仓、健康巡查或 Loo皇谕令。 |
| MQA-007 | 总览 | 总览账户跳转 | 在总览页点击任一重点账户。 | 进入账户详情；标题/账户名称与点击项一致。 |
| MQA-008 | 总览 | 总览持仓跳转 | 在总览页点击任一头部持仓。 | 进入持仓详情；代码、名称、账户信息与点击项一致。 |
| MQA-009 | 组合 | 组合页数据 | 打开 `组合`。 | 显示组合健康、账户类型分布、账户实例分布、资产类别、账户列表、持仓列表。 |
| MQA-010 | 组合 | 下拉刷新 | 在组合页下拉刷新。 | 页面重新加载，不出现重复卡片或错误状态。 |
| MQA-011 | 组合 | 账户详情跳转 | 在组合页点击账户。 | 进入账户详情；能看到事实、表现、配置、健康或持仓列表。 |
| MQA-012 | 组合 | 持仓详情跳转 | 在组合页点击持仓。 | 进入持仓详情；能看到估值、数量、报价、市场数据、健康摘要。 |
| MQA-013 | 账户详情 | 查看关联页面 | 在账户详情点击持仓或 Health 相关入口。 | 能进入对应持仓详情或账户 Health 页面；返回后状态正常。 |
| MQA-014 | 账户详情 | 编辑账户入口 | 点击账户详情右上编辑按钮，打开后取消。 | 编辑弹窗正常展示；取消后不改变原数据。 |
| MQA-015 | 持仓详情 | 查看账户/标的 | 在持仓详情点击 `查看账户` 和 `查看标的`。 | 分别进入账户详情和标的详情；返回链路正常。 |
| MQA-016 | 持仓详情 | 编辑持仓入口 | 点击持仓详情编辑按钮，打开后取消。 | 编辑表单正常展示；取消后不改变原数据。 |
| MQA-017 | 标的详情 | 标的数据 | 打开任一标的详情。 | 显示代码、名称、价格、市场数据、组合角色、相关持仓或账户持有情况。 |
| MQA-018 | 标的身份 | CAD/US 隔离 | 对比一个美股正股和 CAD/CDR/hedged 版本。 | 页面必须保留 `symbol + exchange + currency` 语义，不得把 CAD 版本混成 USD 正股。 |
| MQA-019 | AI | AI 标的快扫 | 在标的详情点击 `AI 标的快扫`。 | 返回中文分析卡片；包含摘要、风险、机会/行动项、免责声明。 |
| MQA-020 | AI | AI 标的身份 | 对 CAD/US 相近标的分别触发快扫。 | AI 分析基于当前标的身份，不得只按 ticker 混淆。 |
| MQA-021 | Health | 全组合 Health | 从组合健康卡进入 Health Score。 | 显示健康分、驱动因素、行动建议、资产配置/约束相关信息。 |
| MQA-022 | Health | AI 组合快扫 | 在全组合 Health 页面点击 `AI 组合快扫`。 | 返回组合级中文分析卡片；包含主要风险、配置建议、免责声明。 |
| MQA-023 | Health | AI 账户快扫 | 从账户详情进入该账户 Health 页面，点击 `AI 账户快扫`。 | 返回账户级中文分析卡片；包含账户健康、账户占比、账户/税务提醒、行动项和免责声明。 |
| MQA-024 | Health | 账户评分口径 | 从账户详情进入账户 Health 页面。 | 顶部显示 `账户内适配 + 全组合目标参考`；文案说明单个账户不必复制全组合目标。 |
| MQA-025 | Health | 配置偏离文案 | 查看 Health 风险护栏/维度里目标配置说明。 | 若当前占比高于目标，文案必须写 `高于目标`；低于目标才写 `低于目标`，不得出现 `目标 22%，当前只有 74%` 这类表达。 |
| MQA-026 | AI | 快扫缓存/刷新回归 | 生成一次 AI 标的/组合/账户快扫后，点击 `重新生成`，再返回上一页并重新进入同一分析入口。 | 首次生成正常；`重新生成` 可再次返回结果且按钮生成中禁用；返回重进后不会接口失败、白屏或身份混淆。 |
| MQA-027 | Health | 资产类别下钻 | 从组合页点击资产类别卡片。 | 进入资产类别详情；显示目标、当前、偏离、相关持仓或建议。 |
| MQA-028 | 发现 | 发现页读取 | 打开 `推荐` tab，点击 `搜索标的`。 | 进入标的发现台；显示搜索框、搜索按钮和当前观察列表。 |
| MQA-029 | 发现 | 搜索有效标的 | 搜索 `VFV`、`AAPL` 或 `Amazon`。 | 返回候选结果；每条结果显示 symbol、name、exchange、currency、type 或 provider。 |
| MQA-030 | 发现 | 搜索结果打开详情 | 点击搜索结果的 `打开详情`。 | 进入标的详情页；详情请求保留当前结果的 symbol、exchange、currency。 |
| MQA-031 | 发现 | 观察列表添加/删除 | 在搜索结果点击 `加入观察`，再点击 `移出观察`。 | 状态切换成功；当前观察列表 chip 同步更新。 |
| MQA-032 | 发现 | CAD/US 搜索身份隔离 | 搜索同一底层公司的 CAD/CDR/hedged 版本与 US 正股。 | 结果必须展示不同 exchange/currency；进入详情不得只按 ticker 混淆。 |
| MQA-033 | 推荐 | 推荐页读取 | 打开 `推荐`。 | 显示推荐摘要、偏好上下文、观察列表、优先事项、情景比较或备注。 |
| MQA-034 | 推荐 | 生成推荐 | 点击重新生成推荐，输入有效 CAD 金额。 | 显示成功提示；推荐列表刷新。 |
| MQA-035 | 推荐 | 观察列表添加/删除 | 添加一个观察 symbol，再删除。 | 添加和删除都成功；刷新后状态一致。 |
| MQA-036 | 导入 | 导入页读取 | 打开 `导入`。 | 显示手动导入说明、添加账户、添加持仓、现有账户、导入规则。 |
| MQA-037 | 导入 | 添加账户表单 | 点击添加账户，填写有效账户信息后提交。 | 创建成功；账户出现在现有账户列表。 |
| MQA-038 | 导入 | 添加持仓前置校验 | 若没有账户，点击添加持仓。 | 显示中文提示 `请先添加账户，再添加持仓。` |
| MQA-039 | 导入 | 添加持仓搜索验证 | 有账户时添加持仓，搜索有效标的并选择。 | 表单自动带出或保留 symbol、名称、币种、交易所等身份信息。 |
| MQA-040 | 导入 | CAD/US 持仓导入 | 分别选择 CAD 版本和 US 正股版本。 | 保存时保留各自交易所和币种，不发生 ticker-only 合并。 |
| MQA-041 | 设置 | 显示币种切换 | 在设置页切换 CAD/USD。 | 保存成功；刷新或返回数据页后显示币种语义一致。 |
| MQA-042 | 设置 | 刷新行情 | 在设置页点击 `刷新组合行情`。 | 显示刷新结果；文案包含刷新持仓数、检查标的数或缺失报价数。 |
| MQA-043 | 设置 | 投资偏好读取 | 打开设置页投资偏好卡片。 | 显示风险偏好、目标配置、账户优先级、观察列表或约束摘要。 |
| MQA-044 | 设置 | 引导式偏好 | 点击 `引导`，完成 5 个问题并应用。 | 生成草稿并应用成功；出现中文成功提示。 |
| MQA-045 | 设置 | 编辑投资偏好 | 点击 `编辑`，修改风险偏好或目标配置并保存。 | 保存成功；推荐页后续读取新偏好。 |
| MQA-046 | 设置 | 推荐约束保存 | 点击 `约束`，修改税务策略、账户优先级或规则并保存。 | 保存成功；出现 `推荐约束已保存`。 |
| MQA-047 | 设置 | 偏好标的 chip | 搜索并添加偏好标的。 | 生成 chip；展示 symbol、市场/交易所、币种或名称；刷新后仍存在。 |
| MQA-048 | 设置 | 排除标的 chip | 搜索并添加排除标的。 | 生成 chip；推荐逻辑后续应排除该标的。 |
| MQA-049 | 设置 | AI 最近分析 | 先生成一次 AI 快扫，再进入设置页查看 `AI 最近分析`。 | 显示最近记录，包含快扫类型、标题、时间和摘要；没有记录时显示空状态。 |
| MQA-050 | 设置 | 资产区间校验 | 输入 `US Equity:60-20` 或无效资产类别后保存。 | 保存被阻止；显示中文行级错误。 |
| MQA-051 | 设置 | 退出登录 | 点击 `退出 Loo国` 后刷新页面。 | 回到登录页；不会自动恢复旧 session。 |

## P1 深度回归用例

| ID | 功能域 | 场景 | 步骤 | 预期结果 |
| --- | --- | --- | --- | --- |
| MQA-101 | 错误处理 | 网络中断恢复 | 加载页面后临时断网，再恢复并刷新。 | 显示可理解错误或恢复数据，不永久白屏。 |
| MQA-102 | 会话 | 401 自动刷新 | 长时间停留后重新打开数据页。 | access token 过期时自动 refresh 并重试；refresh 失败则回登录。 |
| MQA-103 | 数据一致性 | 多页面返回 | 从组合页进入账户、持仓、标的、Health 多层页面后连续返回。 | 返回链路正常；底部 tab 状态不乱。 |
| MQA-104 | 删除保护 | 账户删除弹窗 | 打开账户删除确认弹窗但点取消。 | 不删除数据；返回详情页正常。 |
| MQA-105 | 删除保护 | 持仓删除弹窗 | 打开持仓删除确认弹窗但点取消。 | 不删除数据；返回详情页正常。 |
| MQA-106 | 推荐 | 无效推荐金额 | 生成推荐时输入 0、负数或非数字。 | 不生成推荐；不崩溃。 |
| MQA-107 | 导入 | 数值字段校验 | 添加账户/持仓时输入空值、负数或非数字。 | 表单阻止提交或后端返回中文错误。 |
| MQA-108 | 导入 | CSV 非目标 | 检查导入页是否出现 CSV 上传入口。 | 移动端不要求 CSV；页面应强调手动/引导式导入。 |
| MQA-109 | 设置 | 行情刷新失败 | 网络不稳定时刷新行情。 | 显示错误，不清空现有设置。 |
| MQA-110 | 中文一致性 | 浏览所有 tab 和弹窗。 | 关键流程默认中文；不出现影响操作理解的英文主文案。 |
| MQA-111 | 布局 | 小屏滚动 | 在手机竖屏滚动所有主要页面。 | 内容可触达；按钮不被底部导航遮挡；无横向滚动。 |
| MQA-112 | iOS Web 兼容 | 用 iPhone Safari 打开 URL。 | 页面能加载并登录；若 Web secure storage 限制导致异常，记录为兼容缺陷。 |

## 当前已知缺口与非目标

- 手机端暂不恢复 CSV 导入；移动端优先保留手动导入和校验流程。
- AI 分析当前是产品内快扫卡片，不是长对话聊天机器人。
- AI 快扫不承诺实时新闻、Reddit、论坛或外部研究深度检索。
- 账户级 AI 快扫已进入当前测试范围；持仓级专页 AI 深度分析、走势图全量交互仍属于后续增强项。
- Cloudflare quick tunnel 是临时测试入口，不保证长期 URL 稳定。

## 故障排查

- 看到 `530 The origin has been unregistered from Argo Tunnel`：当前 Cloudflare 临时隧道失效，需要重启远程预览栈并获取新 URL。
- 看到 `failed to load`：先刷新页面；仍失败则检查 Flutter 静态服务和 tunnel 是否在运行。
- 登录后数据页 `401`：优先检查 token/session；确认不是浏览器旧缓存或旧 URL。
- AI 快扫报错：记录标的、页面、时间和错误文案，优先检查 `/api/mobile/analysis/quick-scan` 后端响应。
- 行情刷新结果异常：重点检查 symbol、exchange、currency 是否被保留。

## 测试记录模板

```text
设备：
浏览器：
URL：
账号：
测试时间：

通过：
失败：
阻塞：

失败项：
- ID：
  实际结果：
  截图：
  复现概率：
```

## 签收清单

- P0 用例全部 `Pass` 或有明确缺陷记录。
- CAD/US 标的身份隔离至少手动验证一次。
- AI 标的快扫、AI 组合快扫和 AI 账户快扫都至少成功运行一次，并能在设置页看到最近分析记录。
- 手动导入账户和持仓至少各成功一次。
- 设置页显示币种、行情刷新、投资偏好、推荐约束都完成一次回归。
- 完成测试后，把失败项按优先级写回开发任务列表。
